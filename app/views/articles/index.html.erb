

<div class="container">
  <% @articles.each do |article| %>
    <div class="article_listed">
      <h2><%= link_to article.title, article_path(article), class: "link" %></h2>
      <p><%= article.content[0..100] %></p>
    </div>
  <% end %>
</div>
